{
  "name": "gitpulse-agent",
  "version": "1.0.0",
  "description": "An AI agent that automatically reviews GitHub pull requests using Groq + MCP",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "build": "npm install",
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "groq-sdk": "^0.9.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
